function CooperTest(){
};

CooperTest.prototype.evaluate(obj) {
  var gender = obj.gender;
  var age = obj.age;
  var distance = obj.distance;
  switch()
}

function setTestResult (obj, value){
  if (obj.)
}
